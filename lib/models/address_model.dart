import 'package:flutter/material.dart';

class AddressModel {
  bool isSelected;
  String address;
  String name;
  String number;

  AddressModel({this.isSelected, this.address, this.name, this.number});
}
